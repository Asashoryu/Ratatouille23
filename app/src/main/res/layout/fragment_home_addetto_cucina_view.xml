<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.HomeAddettoCucinaFragment" >

    <data>
        <variable
            name="homeAddettoCucinaViewModel"
            type="com.rat.ratatouille23.viewmodel.HomeAddettoCucinaViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <Button
            android:id="@+id/btn_dispensa"
            android:layout_width="233dp"
            android:layout_height="33dp"
            android:text="dispensa"
            android:onClick="@{() -> homeAddettoCucinaViewModel.setVaiAllaDispensa()}"
            android:textColor="@android:color/black"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/btn_associa_ingredienti"
            android:layout_width="233dp"
            android:layout_height="33dp"
            android:text="Associa ingredienti"
            android:onClick="@{() -> homeAddettoCucinaViewModel.setVaiAdAssociaIngredienti()}"
            android:textColor="@android:color/black"
            tools:ignore="HardcodedText" />

    </LinearLayout>
</layout>